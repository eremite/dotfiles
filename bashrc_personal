HISTSIZE=100000
HISTFILESIZE=20000

# Git tab completion
file=~/dotfiles/git-completion.bash && test -f $file && source $file
__git_complete g __git_main # Enable it for `g` alias

# Git prompt
file=~/dotfiles/git-prompt.sh && test -f $file && source $file
GIT_PS1_SHOWDIRTYSTATE=true
GIT_PS1_SHOWSTASHSTATE=true
GIT_PS1_SHOWUNTRACKEDFILES=true
GIT_PS1_SHOWUPSTREAM="verbose"
PROMPT_COMMAND='__git_ps1 "\w" "\\\$ "'

# Shortcut for getting to meta
meta() {
  current_directory=`basename $(pwd)`
  if [ -d "$META/$current_directory" ]; then
    cd "$META/$current_directory"
  else
    cd $META
  fi
}

# Heroku Toolbelt
export PATH="/usr/local/heroku/bin:$PATH"
